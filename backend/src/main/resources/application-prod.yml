server:
  port: ${PORT:8080}

spring:
  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.walletplatform: INFO
    org.springframework.security: WARN
